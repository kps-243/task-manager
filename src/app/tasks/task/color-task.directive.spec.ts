import { ColorTaskDirective } from './color-task.directive';

describe('ColorTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTaskDirective();
    expect(directive).toBeTruthy();
  });
});
